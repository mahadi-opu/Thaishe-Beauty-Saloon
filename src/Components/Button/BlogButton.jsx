import React from 'react';

const BlogButton = () => {
    return (
        <div>
            <button className='btn blog__btn'> Read More </button>
        </div>
    );
};

export default BlogButton;